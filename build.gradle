plugins {
    id 'org.jetbrains.kotlin.jvm' version '2.1.20'
}

plugins {
    id 'java'
    id 'jacoco'
    //   id 'org.jetbrains.kotlin.jvm' version '1.3.72'
}
group = 'ru.netology'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation("org.jetbrains.kotlin:kotlin-test")
    testImplementation("junit:junit:4.13.2") // Для JUnit 4
}

test {
    useJUnitPlatform()
}
kotlin {
    jvmToolchain(23)
}